# Pinned, not floating.
#
# These were all `>=` before, which meant a `docker compose build` months apart
# could silently pull a new major web3 / eth-account and change how orders are
# signed. For a bot that moves real money, the build must be reproducible; bump
# these deliberately and re-run the test suite, rather than discovering a
# breaking change at execution time.
#
# Versions below are the ones the current test suite passes against.

# Core async HTTP client
aiohttp==3.13.2

# Ethereum and crypto dependencies (perpetuals API request signing)
web3==7.14.0
eth-account==0.13.7
eth-abi==5.2.0

# Environment configuration
python-dotenv==1.2.1

# Terminal UI
colorama==0.4.6

# Development and testing
pytest==9.0.1
pytest-asyncio==1.3.0
